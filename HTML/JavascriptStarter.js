// Javascript Basics
test1 = 3;

let Person = new Object();
Person.name = 'Adam';
Person.age = 20;

console.log(Person.name, Person.age);

let fruits = ['Apple', 'Banana', 'Pear', 'Melon'];
console.log(fruits);

// 1 - if
// 2 - if
// 3 - else

function print(a, b) {
    console.log(a, b);   // The function returns the product of p1 and p2
  }